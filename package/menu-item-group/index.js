import MiMenuItemGroup from '../menu/src/menu-item-group';

/* istanbul ignore next */
MiMenuItemGroup.install = function(Vue) {
  Vue.component(MiMenuItemGroup.name, MiMenuItemGroup);
};

export default MiMenuItemGroup;
