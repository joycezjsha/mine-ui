import MiCheckboxGroup from '../checkbox/src/checkbox-group.vue';

/* istanbul ignore next */
MiCheckboxGroup.install = function(Vue) {
  Vue.component(MiCheckboxGroup.name, MiCheckboxGroup);
};

export default MiCheckboxGroup;
