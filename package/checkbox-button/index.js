import MiCheckboxButton from '../checkbox/src/checkbox-button.vue';

/* istanbul ignore next */
MiCheckboxButton.install = function(Vue) {
  Vue.component(MiCheckboxButton.name, MiCheckboxButton);
};

export default MiCheckboxButton;
