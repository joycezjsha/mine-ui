import MiInputNumber from './src/input-number';

/* istanbul ignore next */
MiInputNumber.install = function(Vue) {
  Vue.component(MiInputNumber.name, MiInputNumber);
};

export default MiInputNumber;
