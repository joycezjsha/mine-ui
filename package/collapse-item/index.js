import MiCollapseItem from '../collapse/src/collapse-item.vue';

/* istanbul ignore next */
MiCollapseItem.install = function(Vue) {
  Vue.component(MiCollapseItem.name, MiCollapseItem);
};

export default MiCollapseItem;
